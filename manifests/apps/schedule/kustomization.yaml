apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - kustomconfig.yaml
resources:
  - role.yaml
  - role-binding.yaml
  - workflow-template.yaml
  - cron-workflow.yaml
labels:
  - pairs:
      app.kubernetes.io/managed-by: kustomize
      environment: prod
images:
  - name: sched
    newName: ghcr.io/pauldotyu/scale23x-demo/sched
    newTag: 0.1.0-beta
