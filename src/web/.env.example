# Environment variables for Frontend (local development)

# API Gateway URL - used by server-side API routes in app/api/chat/route.ts
# For local development, this should point to your local API Gateway
AGENT_SERVICE_URL=http://localhost:8001

# Optional: Next.js specific (NEXT_PUBLIC_* variables are exposed to the browser)
# NEXT_PUBLIC_APP_NAME="AI Chat Demo"
